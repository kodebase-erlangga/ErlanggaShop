<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="Erlangga Shop"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LoginActivity"
            android:exported="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <activity android:name=".DetailActivity" />

        <activity
            android:name=".SignupActivity"
            android:exported="false" />

        <!-- Menambahkan WebView Activities -->
        <activity android:name=".WebViewActivity1"/>
        <activity android:name=".WebViewActivity2"/>
        <activity android:name=".WebViewActivity3"/>
        <activity android:name=".WebViewActivity4"/>
        <activity android:name=".WebViewActivity5"/>
        <activity android:name=".WebViewActivity6"/>
        <activity android:name=".WebViewActivity7"/>
        <activity android:name=".WebViewActivity8"/>

    </application>
</manifest>
